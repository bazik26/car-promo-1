# 🚀 Деплой car-promo-1 на Railway

## 📋 Быстрый Деплой

### Вариант 1: Через Railway Dashboard (Рекомендуется)

1. Зайди на [railway.app](https://railway.app)
2. Нажми **"New Project"**
3. Выбери **"Deploy from GitHub repo"**
4. Найди репозиторий **`bazik26/car-promo-1`**
5. Railway автоматически:
   - Определит Next.js проект
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит сервер (`npm start`)

### Вариант 2: Через Railway CLI

```bash
# Установи Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войди в аккаунт
railway login

# Перейди в директорию проекта
cd /Users/maksnalyvaiko/personal/cars/car-promo-1

# Инициализируй Railway проект
railway init

# Задеплой
railway up
```

## ⚙️ Конфигурация

### Файлы для Railway:

✅ **railway.json** — основная конфигурация
```json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

✅ **Procfile** — команда запуска
```
web: npm start
```

✅ **next.config.js** — оптимизация
- `output: 'standalone'` — минимизирует размер
- `images.unoptimized: true` — для Railway

✅ **package.json** — переменная PORT
- `"start": "next start -p ${PORT:-3003}"`
- Railway автоматически установит PORT

## 🌍 После Деплоя

Railway выдаст URL типа:
```
https://car-promo-1-production.up.railway.app
```

### Настрой Кастомный Домен (опционально):

1. В Railway Dashboard → Settings → Domains
2. Нажми **"Generate Domain"** или **"Custom Domain"**
3. Если свой домен — добавь CNAME запись:
   ```
   promo.твойдомен.ru → [railway-domain].up.railway.app
   ```

## 🔧 Environment Variables

Railway автоматически установит:
- `PORT` — порт для сервера
- `NODE_ENV=production` — production режим

## 📊 Мониторинг

В Railway Dashboard можешь отслеживать:
- 📈 **Metrics** — CPU, RAM, Network
- 📝 **Logs** — логи приложения
- 🔄 **Deployments** — история деплоев
- ⚙️ **Settings** — настройки проекта

## 🔄 Автоматический Деплой

Railway автоматически задеплоит при каждом пуше в `main`:

```bash
git add .
git commit -m "update: ..."
git push origin main
```

→ Railway автоматически соберет и задеплоит новую версию! ✨

## ✅ Checklist После Деплоя

- [ ] Проверь URL открывается
- [ ] Проверь что картинки загружаются
- [ ] Проверь таймер работает
- [ ] Проверь scroll snap на мобильном
- [ ] Проверь кнопки звонка/WhatsApp
- [ ] Добавь домен в car-api CORS (если будет форма)

## 🎯 Готово!

Твой TikTok-style промо-лендинг готов к работе! 🚗⚡

---

**Локально:** http://localhost:3003  
**Railway:** https://car-promo-1-production.up.railway.app (после деплоя)

